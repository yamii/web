'use strict';

describe( 'index test', function () {

require( 'should' );

	it ( 'should be running test', function () {
		var x = 0;
		x.should.equal( 0 );
	} );

	it ( 'should be editing test', function () {
		var x = 0;
		x.should.equal( 0 );
	} );

} );